Напишите SQL-запрос, который составит поле вида <name> — <professions> из таблицы persons. 

Будьте внимательны: между именем и профессией стоит пробел, дефис и пробел.

Назовите результирующее поле about.

SELECT concat(name, ' - ', professions) as about
FROM   persons;